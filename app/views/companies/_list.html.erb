<div class="dragdropbox">
<ol id="<%= parent_id %>" >
    <% companies.each do |c| %>
        <li id="<%= "#{parent_id}_#{c.id}" %>" class="draggable list_item"><%= c.name %></li>
    <% end %>
</ol>
</div>