<% content_for :header_tags do %>
    <%= stylesheet_link_tag 'CRMstyle', :plugin => 'crm' %>
<% end %>
<div class='contextual'>
  <%= link_to t(:new, :model => t(:company)),new_company_path, :class => 'icon icon-add' %>
  <%= link_to t(:new, :model => t(:client)),new_client_path, :class => 'icon icon-add' %>
</div>
<h2> CRM </h2>

<fieldset class='collapsible collapsed'>
  <legend onclick='toggleFieldset(this);'>Import CSV</legend>
  <div style="display: none;">
    <%= form_tag import_companies_path, multipart: true do %>
        <%= file_field_tag :file %>
        <%= submit_tag "Company Import" %>
    <% end %>
    <br>
    <%= form_tag import_clients_path, multipart: true do %>
        <%= file_field_tag :file %>
        <%= submit_tag "Client Import" %>
    <% end %>
  </div>
</fieldset>

<%= render 'search' %>

<h3><%= t('activerecord.models.company.other') %></h3>

<div style="width:70%">
<% @companies.each do |company| %>
    <%= render company %>
      <fieldset class='collapsible collapsed'>
        <legend onclick='toggleFieldset(this);'><%= t('activerecord.models.client.other')%></legend>
    <div style="display: none;">
    <% if company.clients.empty? %>
        <p>No contacts created yet</p>
    <% else %>

        <table class='customertable'>
          <%= render 'clients/header' %>
          <%= render company.clients %>
        </table>
    <% end %>
    </div>
    </fieldset>

    <hr>
<% end %>
</div>