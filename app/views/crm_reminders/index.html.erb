<h2><%= CrmReminder.model_name.human(count:2)%></h2>
<% @crm_reminders.group_by(&:remindable_type).each do |group, reminder| %>
  <div>
    <h3><%= group %></h3>
    <%= render reminder %>
  </div>
<% end %>

